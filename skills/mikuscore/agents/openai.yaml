interface:
  display_name: "mikuscore"
  short_description: "MusicXML-canonical and ABC-AI handoff guidance for mikuscore"
  default_prompt: "Use $mikuscore only for mikuscore-specific score conversion, diagnostics, AI handoff, and CLI workflows. Treat mikuscore as opt-in: start only when the user explicitly names mikuscore, asks for mikuscore-specific behavior, or the conversation is already inside an active mikuscore workflow. Do not trigger from MusicXML, ABC, MIDI, MuseScore, MEI, LilyPond, VSQX, xml, or score-conversion words alone. Keep the explanation anchored to mikuscore's product contract: canonical score source remains MusicXML, while current generative-AI full-score handoff and new-score generation use ABC. Prefer MusicXML as the canonical internal explanation axis for format conversion, but prefer ABC when the question is specifically about current AI-facing score exchange. Distinguish supported versus experimental scope clearly, and prefer vendor/mikuscore/docs/spec/* plus vendor/mikuscore/docs/AI_INTERACTION_POLICY.md over README-level summaries when there is any conflict. Use mikuscore for conversion-path guidance, diagnostics interpretation, format-positioning guidance, AI handoff guidance, and CLI/build/test workflow guidance. Do not present mikuscore as a feature-complete score engraving editor, and do not imply guarantees or repair behaviors that its specs do not define."
